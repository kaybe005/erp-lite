# PostgreSQL connection string for the bundled Docker Compose database
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/erp_lite?schema=public"

# Public base URL for local development
NEXTAUTH_URL="http://localhost:3000"

# Secret for authentication
NEXTAUTH_SECRET="replace-with-a-long-random-secret"
